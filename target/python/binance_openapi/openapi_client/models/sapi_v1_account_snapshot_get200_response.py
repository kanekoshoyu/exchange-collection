# coding: utf-8

"""
    Binance Public Spot API

    OpenAPI Specifications for the Binance Public Spot API  API documents:   - [https://github.com/binance/binance-spot-api-docs](https://github.com/binance/binance-spot-api-docs)   - [https://binance-docs.github.io/apidocs/spot/en](https://binance-docs.github.io/apidocs/spot/en)

    The version of the OpenAPI document: 1.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import json
import pprint
from pydantic import BaseModel, ConfigDict, Field, StrictStr, ValidationError, field_validator
from typing import Any, List, Optional
from openapi_client.models.snapshot_futures import SnapshotFutures
from openapi_client.models.snapshot_margin import SnapshotMargin
from openapi_client.models.snapshot_spot import SnapshotSpot
from pydantic import StrictStr, Field
from typing import Union, List, Set, Optional, Dict
from typing_extensions import Literal, Self

SAPIV1ACCOUNTSNAPSHOTGET200RESPONSE_ONE_OF_SCHEMAS = ["SnapshotFutures", "SnapshotMargin", "SnapshotSpot"]

class SapiV1AccountSnapshotGet200Response(BaseModel):
    """
    SapiV1AccountSnapshotGet200Response
    """
    # data type: SnapshotSpot
    oneof_schema_1_validator: Optional[SnapshotSpot] = None
    # data type: SnapshotMargin
    oneof_schema_2_validator: Optional[SnapshotMargin] = None
    # data type: SnapshotFutures
    oneof_schema_3_validator: Optional[SnapshotFutures] = None
    actual_instance: Optional[Union[SnapshotFutures, SnapshotMargin, SnapshotSpot]] = None
    one_of_schemas: Set[str] = { "SnapshotFutures", "SnapshotMargin", "SnapshotSpot" }

    model_config = ConfigDict(
        validate_assignment=True,
        protected_namespaces=(),
    )


    def __init__(self, *args, **kwargs) -> None:
        if args:
            if len(args) > 1:
                raise ValueError("If a position argument is used, only 1 is allowed to set `actual_instance`")
            if kwargs:
                raise ValueError("If a position argument is used, keyword arguments cannot be used.")
            super().__init__(actual_instance=args[0])
        else:
            super().__init__(**kwargs)

    @field_validator('actual_instance')
    def actual_instance_must_validate_oneof(cls, v):
        instance = SapiV1AccountSnapshotGet200Response.model_construct()
        error_messages = []
        match = 0
        # validate data type: SnapshotSpot
        if not isinstance(v, SnapshotSpot):
            error_messages.append(f"Error! Input type `{type(v)}` is not `SnapshotSpot`")
        else:
            match += 1
        # validate data type: SnapshotMargin
        if not isinstance(v, SnapshotMargin):
            error_messages.append(f"Error! Input type `{type(v)}` is not `SnapshotMargin`")
        else:
            match += 1
        # validate data type: SnapshotFutures
        if not isinstance(v, SnapshotFutures):
            error_messages.append(f"Error! Input type `{type(v)}` is not `SnapshotFutures`")
        else:
            match += 1
        if match > 1:
            # more than 1 match
            raise ValueError("Multiple matches found when setting `actual_instance` in SapiV1AccountSnapshotGet200Response with oneOf schemas: SnapshotFutures, SnapshotMargin, SnapshotSpot. Details: " + ", ".join(error_messages))
        elif match == 0:
            # no match
            raise ValueError("No match found when setting `actual_instance` in SapiV1AccountSnapshotGet200Response with oneOf schemas: SnapshotFutures, SnapshotMargin, SnapshotSpot. Details: " + ", ".join(error_messages))
        else:
            return v

    @classmethod
    def from_dict(cls, obj: Union[str, Dict[str, Any]]) -> Self:
        return cls.from_json(json.dumps(obj))

    @classmethod
    def from_json(cls, json_str: str) -> Self:
        """Returns the object represented by the json string"""
        instance = cls.model_construct()
        error_messages = []
        match = 0

        # deserialize data into SnapshotSpot
        try:
            instance.actual_instance = SnapshotSpot.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        # deserialize data into SnapshotMargin
        try:
            instance.actual_instance = SnapshotMargin.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        # deserialize data into SnapshotFutures
        try:
            instance.actual_instance = SnapshotFutures.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))

        if match > 1:
            # more than 1 match
            raise ValueError("Multiple matches found when deserializing the JSON string into SapiV1AccountSnapshotGet200Response with oneOf schemas: SnapshotFutures, SnapshotMargin, SnapshotSpot. Details: " + ", ".join(error_messages))
        elif match == 0:
            # no match
            raise ValueError("No match found when deserializing the JSON string into SapiV1AccountSnapshotGet200Response with oneOf schemas: SnapshotFutures, SnapshotMargin, SnapshotSpot. Details: " + ", ".join(error_messages))
        else:
            return instance

    def to_json(self) -> str:
        """Returns the JSON representation of the actual instance"""
        if self.actual_instance is None:
            return "null"

        if hasattr(self.actual_instance, "to_json") and callable(self.actual_instance.to_json):
            return self.actual_instance.to_json()
        else:
            return json.dumps(self.actual_instance)

    def to_dict(self) -> Optional[Union[Dict[str, Any], SnapshotFutures, SnapshotMargin, SnapshotSpot]]:
        """Returns the dict representation of the actual instance"""
        if self.actual_instance is None:
            return None

        if hasattr(self.actual_instance, "to_dict") and callable(self.actual_instance.to_dict):
            return self.actual_instance.to_dict()
        else:
            # primitive type
            return self.actual_instance

    def to_str(self) -> str:
        """Returns the string representation of the actual instance"""
        return pprint.pformat(self.model_dump())


